import React from 'react';
import ReactDOM from 'react-dom';
import SampleToDoListApp from './SampleToDoListApp';

ReactDOM.render(
  <React.StrictMode>
    <SampleToDoListApp/>
  </React.StrictMode>,
  document.getElementById('root')
);
